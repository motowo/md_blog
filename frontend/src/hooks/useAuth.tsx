// Re-export from the contexts directory to maintain backward compatibility
// eslint-disable-next-line react-refresh/only-export-components
export { useAuth, AuthProvider } from '../contexts/AuthContext';
